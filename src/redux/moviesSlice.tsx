// import { createSlice } from '@reduxjs/toolkit'

// const initialState = {
//   entities: normalizedMovies.reduce((acc, item) => {
//     acc[itemm.id] = item

//     return acc
//   }, {})
//   ids: normalizedMovies.map(id => id)
// }

// export const moviesSlice = createSlice({
//   name: 'movies',
//   initialState,
//   reducers: {}
// })
